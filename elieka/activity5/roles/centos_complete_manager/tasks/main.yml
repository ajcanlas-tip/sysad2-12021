---
# tasks file for roles/centos_complete_manager
  - name: Installing Java
    yum:
      name: java-1.8.0-openjdk
      state: latest
      update_cache: YES

  - name: Installing boto,ansible, and openstack
    pip:
      name:
        - boto
        - ansible
        - python-openstackclient
