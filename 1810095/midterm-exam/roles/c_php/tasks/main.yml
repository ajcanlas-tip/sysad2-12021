---
# tasks file for roles/c_php
- name: installing php in centos
  yum:
    name: php
    state: latest
    update_cache: yes

- name: php host index
  copy:
    content: "moreno host index"
    dest: /var/www/html/index2.html
    owner: root
    group: root
    mode: '0644'
