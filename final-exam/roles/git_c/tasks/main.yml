---
# tasks file for roles/git_c
- name: Install Git in Centos
  yum:
    name: git
    state: latest

- name: Clone repository
  git:
    repo: "{{ git_repo }}"
    dest: "{{ git_dest }}"
    version: "{{ git_ver }}"
