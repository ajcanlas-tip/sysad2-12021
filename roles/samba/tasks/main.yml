---
# tasks file for samba
- name: Install samba service in Ubuntu
  apt:
    name: samba
    state: present
  when: ansible_facts['distribution'] == 'Ubuntu'

- name: Install samba service in CentOS
  yum:
    name: samba
    state: present
  when: ansible_facts['distribution'] == 'CentOS'

