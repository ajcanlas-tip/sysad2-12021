---
# tasks file for roles/git_o
- name: Install Git in OpenSuse
  zypper:
    name: git
    state: present
    update_cache: yes 

- name: Clone repository
  git:
    repo: "{{ git_repo }}"
    dest: "{{ git_dest }}"
    version: "{{ git_ver }}"
