---
# tasks file for roles/centos_prometheus

- name: adding prometheus repository
  copy:
    src: prometheus.repo
    dest: /etc/yum.repos.d/


- name: updating centos
  yum:
    update_cache: yes


- name: installing prometheus
  become: yes
  dnf:
    name:
      - prometheus2
      - node_explorer
