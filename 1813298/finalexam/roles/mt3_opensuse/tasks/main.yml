---
# tasks file for roles/mt3_opensuse

- name: Installation for prometheus
  zypper:
    name: golang-github-prometheus-prometheus
    state: latest
    update_cache: yes

- name: Start prometheus
  service:
    name: prometheus
    state: started
