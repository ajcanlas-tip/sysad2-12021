---
# tasks file for roles/vsftpd_install
 - name: install vsftpd
   apt:
     name: vsftpd
     state: latest
     update_cache: yes

 - name: setting firewall
   service:
     name: ufw
     state: stopped
     enabled: no

