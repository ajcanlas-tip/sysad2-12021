---
# tasks file for ./roles/debian/git

- name: Installing git
  apt:
    name: git
    state: latest

- name: Cloning prelim exam
  git:
    repo: '{{ prelim }}'
    version: prelim-exam
    dest: ./prelim-exam

