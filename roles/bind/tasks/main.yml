---
# tasks file for bind
- name: Install bind9 service in Ubuntu
  apt:
    name: bind9
    state: present
  when: ansible_facts['distribution'] == 'Ubuntu'

- name: Install bind9 service in CentOS
  yum:
    name: bind
    state: present
  when: ansible_facts['distribution'] == 'CentOS'

