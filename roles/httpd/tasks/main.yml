---
# tasks file for httpd
- name: Install httpd service in Ubuntu
  apt:
    name: apache2
    state: present
  when: ansible_facts['distribution'] == 'Ubuntu'

- name: Install httpd service in CentOS
  yum:
    name: httpd
    state: present
  when: ansible_facts['distribution'] == 'CentOS'
  
