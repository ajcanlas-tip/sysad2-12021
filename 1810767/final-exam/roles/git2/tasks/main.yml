---
# tasks file for git2
- name: Create a directory
  file:
    path: /home/ajbindoy-tip/repo
    state: directory
    mode: "777"

- name: Clone the prelim repository
  git:
    repo: 'https://github.com/ajbindoy-tip/sysad2-12021.git'
    dest: /home/ajbindoy-tip/repo
    version: prelim-exam
    clone: yes
    update: yes
