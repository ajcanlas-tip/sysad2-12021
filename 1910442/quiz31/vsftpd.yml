---
 - name: Install vsftpd server
   hosts: ubuntu
   tasks: 
    - name: Installation of vsftpd 
      apt:
        name: vsftpd
        state: present
        update_cache: yes
      when: ansible_facts['distribution'] == 'Ubuntu'
    - name: vsftpd in ubuntu
      apt:
        name: vsftpd
        state: present
        update_cache: yes
      when: ansible_facts['distribution'] == 'Ubuntu'


