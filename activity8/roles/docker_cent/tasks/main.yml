---
# tasks file for roles/docker_cent

- name: install docker file
  yum:
    name: docker
    state: latest
    update_cache: yes
  register: docker

- name: install docker .py
  pip:
    name: docker.py
    state: latest
  when: docker.changed
