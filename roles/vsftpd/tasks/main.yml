---
# tasks file for vsftpd
- name: Install vsftpd service for Ubuntu
  apt:
    name: vsftpd
    state: present
  when: ansible_facts['distribution'] == 'Ubuntu'

- name: Install vsftpd service for CentOS
  yum:
    name: vsftpd
    state: present
  when: ansible_facts['distribution'] == 'CentOS'
