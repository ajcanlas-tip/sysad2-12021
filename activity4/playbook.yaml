---
 - name: Installation
   hosts: Ubuntu

   tasks:
   - name: Installing boto, ansible, openstack thru pip
     pip:
        name:
         - boto
         - ansible
         - openstackclient

   tasks:
   - name: Ubuntu java install
     apt:
       name: default-jre
       state: present
       update_cache: yes
