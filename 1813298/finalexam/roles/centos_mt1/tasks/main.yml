---
# tasks file for roles/centos_mt1

- name: Add prometheus repo
  copy:
    src: prometheus.repo
    dest: /etc/yum.repos.d/

- name: Installation for prometheus
  yum:
    name: prometheus2
    state: latest
    update_cache: yes

- name: start prometheus
  service:
    name: prometheus
    state: started

