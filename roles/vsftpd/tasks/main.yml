---
# tasks file for vsftpd
- name: Install vsftpd service in Ubuntu
  apt:
    name: vsftpd
    state: present
  when: ansible_facts['distribution'] == 'Ubuntu'

- name: Install vsftpd service in CentOS
  yum:
    name: vsftpd
    state: present
  when: ansible_facts['distribution'] == 'CentOS'

