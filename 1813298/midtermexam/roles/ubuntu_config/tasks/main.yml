---
# tasks file for roles/ubuntu_config

- name: Installation of Nagios Core Dependencies Packages for Ubuntu
  apt:
    name:
      - build-essential
      - apache2
      - php
      - openssl
      - perl
      - make
      - php-gd
      - libgd-dev
      - libapache2-mod-php
      - libperl-dev
      - libssl-dev
      - daemon
      - wget
      - apache2-utils
      - unzip
    state: latest
    update_cache: yes

- name: Add Group Nagcmd
  group:
    name: nagcmd
    state: present

- name: Add User for Nagios
  user:
    name: nagios
    group: nagcmd
