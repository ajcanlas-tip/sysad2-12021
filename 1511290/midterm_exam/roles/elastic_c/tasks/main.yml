---
# tasks file for roles/elastic_c

- name: install pre-requisite of elastic
  yum:
    name: java-11-openjdk-devel
    state: latest
    update_cache: yes

- name: install elasticsearch repository
  yum_repository:
    name: Elastic_7.X_repo
    baseurl: https://artifacts.elastic.co/packages/7.x/yum
    gpgcheck: yes 
    gpgkey: https://artifacts.elastic.co/GPG-KEY-elasticsearch
    enabled: yes
    description: Elastic 7.x repo

- name: install elasticsearch gpg key
  rpm_key:
    key: https://artifacts.elastic.co/GPG-KEY-elasticsearch
    state: present

- name: Install elasticsearch
  yum:
    name: elasticsearch
    state: present
    update_cache: yes

- name: update config file to allow access
  lineinfile:
    destfile: /etc/elasticsearch/elasticsearch.yml
    regexp: '#network.host: '
    line:  'network.host: localhost '

- name: update config to allow access
  lineinfile:
    destfile: /etc/elasticsearch/elasticsearch.yml
    regexp: '#http.port:'
    line: 'http.port 9200'

- name: update time
  lineinfile:
    dest: /usr/lib/systemd/system/elasticsearch.service
    regexp: 'TimeStartSec='
    line: 'TimeoutStartSec=900'



- name: install logstash repo
  yum_repository:
    name: Elastic repository for 7.x packages
    baseurl: https://artifacts.elastic.co/packages/7.x/yum
    gpgcheck: yes
    gpgkey: https://artifacts.elastic.co/packages/GPG-KEY-elasticsearch
    enabled: yes
    description: Elastic 7.x packages repo

- name: install logstash
  yum:
    name: logstash
    state: present
    update_cache: yes

- name: update config file
  lineinfile:
    dest: /etc/logstash/logstash.yml
    regexp: 'http.host'
    line: 'http.host: 127.0.0.1'

- name: start and enable logstash
  service:
    name: logstash
    state: started
    enabled: yes

- name: install kibana repo
  yum_repository:
    name: Kibana 7.x packages
    baseurl: https://artifacts.elastic.co/packages/7.x/yum
    gpgcheck: yes
    gpgkey: https://artifacts.elastic.co/GPG-KEY-elasticsearch
    enabled: yes
    description: Kibana-7.x

- name: install kibana
  yum:
    name: kibana
    state: present
    update_cache: yes

- name: update config server.host
  lineinfile:
    destfile: /etc/kibana/kibana.yml
    regexp: 'server.host'
    line: 'server.host: 0.0.0.0'

- name: update config server.port
  lineinfile:
    dest: /etc/kibana/kibana.yml
    regexp: 'server.port'
    line: 'server.port: 5601'

- name: enable and start kibana
  service:
    name: kibana
    state: started
    enabled: yes
