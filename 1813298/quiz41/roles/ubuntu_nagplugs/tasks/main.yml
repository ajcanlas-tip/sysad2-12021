---
# tasks file for roles/ubuntu_nagplugs

- name: Installing Nagios Plugins
  unarchive:
    src: https://nagios-plugins.org/download/nagios-plugins-2.3.3.tar.gz
    dest: /tmp
    remote_src: yes

- name: Configuartion Nagios Plugs
  command: ./configure --with-nagios-user=nagios --with-nagios-group=nagcmd
  args:
    chdir: /tmp/nagios-plugins-2.3.3

- name: Configuration MAKE
  make:
    target: install
    chdir: /tmp/nagios-plugins-2.3.3

- name: Configuation using make install
  make:
    target: install
    chdir: /tmp/nagios-plugins-2.3.3

- name: Enabling and Starting Nagios Services
  service:
    name: nagios
    enabled: yes
    state: started

