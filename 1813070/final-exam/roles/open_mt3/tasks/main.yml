---
# tasks file for roles/open_mt3
- name: Installing prometheus
  zypper:
    name: golang-github-prometheus-prometheus
    state: latest
    update_cache: yes

- name: Start prometheus
  service:
    name: prometheus
    state: started
