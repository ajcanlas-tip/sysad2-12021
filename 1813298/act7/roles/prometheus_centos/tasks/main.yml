---
# tasks file for roles/prometheus_centos

- name: Add prometheus repo
  copy:
    src: prometheus.repo
    dest: /etc/yum.repos.d/

- name: Centos update
  yum:
    update_cache: yes

- name: Install prometheus
  become: yes
  dnf:
    name:
      - prometheus2
      - node_exporter
      
